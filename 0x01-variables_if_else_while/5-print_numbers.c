#include <stdio.h>

/**
 * main - Print all single digit numbers from 0
 *
 * Return: 0 - success
 */
int main(void)
{
	int number;

	for (number = 0; number < 10; number++)
		printf("%d\n", number);

	return (0);
}
