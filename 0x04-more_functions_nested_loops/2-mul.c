#include "main.h"

/**
 * mul - find the product of two numbers
 * @a: first integer
 * @b: second integer
 * Return: the product
 */
int mul(int a, int b)
{
	int product;

	product = a * b;
	return (product);
}
